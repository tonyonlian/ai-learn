# 如何写好一个Agent Skills

开发一个优秀的 Agent Skill（智能体技能）不仅仅是写几行调用 API 的代码，更核心的挑战在于如何让 AI “大脑”准确地理解、信任并正确调用这个技能。

当前一个高标准的 Agent Skill 开发应遵循以下四个关键维度：

## 1.极其精准的“提示语工程”（Description is UI）

对于智能体来说，技能的**描述（Description）**就是它的用户界面。

- 反面教材: name: get_weather, description: 获取天气（太模糊，AI 不知道能查哪里的天气，也不知道能查多久以内的）。
- 正面教材: description: 获取指定城市未来 7 天内的天气预报。输入参数必须是标准的城市中文名称（如“北京市”）。支持查询气温、湿度和降水概率。

秘诀: 在描述中加入**“使用场景”**。告诉 AI：“当用户询问户外运动建议或旅行计划时，优先调用此技能。”

## 2. 严格的参数验证与鲁棒性（Robustness）

AI 有时会“幻觉”出一些不存在的参数。一个好的 Skill 必须具备防御性：

- 类型检查：确保日期格式是 YYYY-MM-DD 而不是“下周二”。
- 枚举值约束：如果你的技能只支持“北京”和“上海”，要在参数定义中明确列出 enum: ["北京市", "上海市"]。
- 错误回传：如果 API 调用失败，不要只返回 Error 500。要返回人类（和 AI）能看懂的话：“当前查询的城市超出了服务范围，请尝试查询国内主要城市。” 这样 AI 才能根据错误信息尝试纠正。

## 3. 输出的“AI 友好性”（Output for LLM）

技能返回的数据不是给用户看的，而是给 AI 大脑看的：

- 去粗取精：如果原始 API 返回了 2000 行 JSON，里面只有 3 行是有用的，请在 Skill 执行器里过滤掉冗余数据。过多的无关信息会浪费 Token 并干扰 AI 的判断。
- 结构化：使用 JSON 或 Markdown 表格返回数据。
- 元数据标注：在返回结果中加入状态说明，例如 status: success 或 note: 数据更新于 5 分钟前。

## 4. 安全与权限控制（Security）

Agent Skill 是最容易出安全问题的地方（特别是涉及“动作类”技能时）：

- 确认确认再确认（User-in-the-loop）：对于涉及“扣费”、“删除”、“发送邮件”等敏感操作，技能逻辑中应包含一个requires_confirmation: true 的标志，强制要求用户点击确认后才真正执行。
- 最小权限原则：如果一个 Skill 只需要读取数据库，就不要给它写入权限。

| 阶段   | 关键动作                                     |
|------|------------------------------------------|
| 设计阶段 | 确定该技能解决的原子问题（不要试图做一个“万能技能”）              |
| 定义阶段 | 编写 OpenAPI (Swagger) 规范或 JSON Schema     |
| 实现阶段 | 编写连接器代码（Connector），处理身份认证（OAuth/API Key） |
| 测试阶段 | 边界测试：故意输入模糊指令，看 Agent 是否会误触发或正确追问缺失参数    |


## 5. 一个“好技能”的清单 (Checklist)

- [ ] 3.1 名字是否唯一且具有语义化？
- [ ] 3.2 描述是否包含了“什么时候用”和“什么时候不用”？
- [ ] 3.3 是否处理了超时和网络异常？
- [ ] 3.4 返回的数据量是否精简（控制在 1k Token 以内）？
- [ ] 3.4 是否有明确的示例（Few-shot examples）指导 AI 如何调用？

## 6. 示例
帮我写个git commit 的skills。要求能git commit，提交的信息从文档的变化中摘要中提取，提交信息字数少于50字，skill的鲁棒性很好。

技能清单与 SKILL.md 对标表

| “好技能”清单要求 (Checklist) | SKILL.md中的对应章节                         | 鲁棒性实现方式 (Implementation)                  |
|---------------------|----------------------------------------|-------------------------------------------|
| 1.名字唯一且具备语义化        | ## 1. 技能基本信息 -> ID: git_summary_commit | 使用动宾结构，让 AI 一眼看出这是“Git 摘要提交”              |
| 2. 明确“什么时候用/不用”     | ## 1. 核心功能 & ## 5. 使用示例                | 描述中明确提到“分析暂存区”，暗示未 add 的文件不会被处理           |
| 3. 强制的字数/格式约束       | ## 2. 参数定义 -> max_chars: 50            | 在 Input 层面就锁死上限，并在 Prompt 中反复强调           |
| 4. 异常处理与错误回传        | ## 3. 鲁棒性执行策略 -> 环境预检                  | 定义 E001-E004 错误码，确保 AI 收到报错后能“自愈”或追问用户    |
| 5. 自动化的环境自适应        | ## 2. 参数定义 -> auto_add: boolean        | 允许 AI 根据上下文决定是否自动补全 git add 动作            |
| 6. 输入数据的精简（防溢出      | ## 3. 鲁棒性执行策略 -> Diff 智能截断             | 明确规定超过阈值（如 10KB）时的截断逻辑，防止 Token 爆炸        |
| 7. 安全确认（动作类技能）      | ## 2. 接口定义                             | 建议在框架层开启 require_confirmation，防止误操作提交敏感信息 |