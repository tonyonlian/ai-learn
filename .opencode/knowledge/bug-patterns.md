# 历史 Bug 修复经验库

## 模式 A：异步竞争导致的数据丢失
- **现象**：在高并发保存订单时，偶尔出现状态回滚。
- **原因**：Service 层未加 @Transactional，导致数据库事务未锁定。
- **对策**：必须在所有写操作方法上添加事务注解，并使用自增版本号（Optimistic Locking）。

## 模式 B：Redis 缓存穿透
- **现象**：数据库压力瞬间激增。
- **修复规范**：查询结果为空时，必须缓存一个短时间的空对象（Null Object），防止频繁穿透。

